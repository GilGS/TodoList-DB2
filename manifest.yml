declared-services:
  TodoList-DB2:
    label: dashDB
    plan: entry
applications:
- name: todolist-db2
  memory: 256M
  instances: 2
  random-route: true
  services:
  - TodoList-DB2
  buildpack: https://github.com/IBM-Swift/swift-buildpack.git#develop

